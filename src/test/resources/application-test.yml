server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:shopping}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: create
    #    show-sql: true
    properties:
      hibernate:
        format_sql: false
        jdbc:
          batch_size: 5000
        #        format_sql: true
        #order_inserts: true
        #order_updates: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

jwt:
  secret:
    key: ${SECRET_KEY:WvG406dyLBEesRAwcYAKSA4PicZkO/iBAFB93NLYF4k=}

redis:
  host: ${REDIS_HOST:localhost}
  port: ${REDIS_PORT:6379}

redisson:
  config: |
    singleServerConfig:
      address: "redis://${REDIS_HOST:localhost}:${REDIS_PORT:6379}"
    
    
